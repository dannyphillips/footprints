version: 2

jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout

      - run:
          name: Run Yarn
          command: yarn

      - run:
          name: Run Tests
          command: yarn test

      - save_cache:
          key: dependency-cache
          paths:
            - ~/.cache/yarn